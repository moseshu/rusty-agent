# ra-protocol 的公开 API 基线。由 `cargo xtask public-api --bless` 生成。
# 手改无意义——请改代码后重新生成，并让这份 diff 出现在 code review 里。
mod ra_protocol::control
mod ra_protocol::frame
mod ra_protocol::lifecycle
mod ra_protocol::server
mod ra_protocol::subscribe
mod ra_protocol::transport
mod ra_protocol::transport::stdio
mod ra_protocol::transport::websocket
