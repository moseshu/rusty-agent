[package]
name = "xtask"
description = "仓库任务：schema 字节稳定性、prompt dump、guard 登记表校验、token 预算。"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
clap = { workspace = true }
